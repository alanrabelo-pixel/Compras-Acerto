node_modules/
.next/
.env
*.log
/uploads/
/public/pedidos-compra/
*.tsbuildinfo

# Artefatos do Google Drive para Desktop (backup da pasta local do projeto,
# ver conversa sobre sincronização) — não são arquivos do projeto.
desktop.ini
.tmp.driveupload/
