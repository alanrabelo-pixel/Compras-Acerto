# Registro de trabalho: alAi, 19 de agosto de 2026

Esta sessão foi uma auditoria técnica sênior do alAi pedida pelo dono do sistema, seguida da correção do que a auditoria encontrou. Ao longo de 40 commits, o trabalho passou por seis frentes: limpeza de texto e produção do plano de correção, fechamento das falhas de autorização que impediam qualquer deploy (P0), infraestrutura de erro, log, índices e transação (P1), clareza da interface para quem usa (P2), constraints de banco, trilha de auditoria e achados soltos (P3), e por fim decisões de negócio, mapeamento de tempos por etapa e o levantamento das brechas de leitura que ficou apenas documentado. O sistema não estava em produção em nenhum momento desta sessão.

## O que mudou, por frente

### Revisão de texto e plano de correção

- **9c0f9b0, Varredura de travessão em src/.** 146 arquivos, 568 inserções e 568 deleções, exatamente 1:1. Substitui o caractere U+2014 em comentários (`src/lib/auth.ts`, `src/lib/rbac.ts`, `src/lib/workflow.ts`, `src/lib/integrations/ai.ts`, `src/test-helpers/fixtures.ts`), em copy de interface (os dois maiores blocos são `src/components/StageHistoryPanel.tsx`, 39 linhas reescritas, e `src/components/RequestActions.tsx`, 35), em assuntos de e-mail (`src/lib/integrations/gmail.ts`), nos sete builders de prompt de `src/lib/integrations/ai.ts` (seis de etapa mais `buildRequisitionAssistPrompt`) e nos PDFs (`pedidoCompra.tsx`, `manualProcesso.tsx`, `dashboardSummary.tsx`). Em 45 das 568 linhas o travessão não estava numa frase: era o literal `"<U+2014>"` usado como marcador de célula vazia, e virou hífen simples em 43 casos e `"N/A"` em 2 (`slaCompliancePct` e `slaPct` em `src/app/api/dashboards/export-pdf/route.tsx`), ou seja, mudança visível de glifo em tabelas e dashboards. Também mudou o `returnReason` do payload fixo do botão DEVOLVER em `RequestActions.tsx` (linhas 291 e 391), não o padrão da rota de Triagem, que nunca teve travessão. Nenhuma lógica muda. O problema anterior: o travessão era lido como marca de texto escrito por IA e aparecia em rótulo de tela, mensagem de erro de API, assunto de e-mail, template de Slack e prompt enviado ao modelo. Contagem: de 597 ocorrências em src/ para 42, todas em `src/app/globals.css`, que este commit não tocou apesar de a mensagem afirmar "every occurrence across src/".
- **d678ba9, Travessão em schema, README, CSS, seed e config.** 6 arquivos, 160 inserções e 160 deleções: `CHECKLIST-GOOGLE-OAUTH.md`, `README.md`, `prisma/schema.prisma`, `prisma/seed.ts`, `src/app/globals.css` e `vitest.config.ts`. Fecha o buraco de 9c0f9b0 no globals.css, onde só conteúdo dentro de `/* */` muda. Em `prisma/schema.prisma` nenhum model, campo, enum, índice ou `@db` é alterado (as linhas adicionadas que não são comentário são declarações de campo com comentário no fim: `suggestedDeadline`, `level`, `personifiedBy`, `localEntrega`). Ressalva importante: em `prisma/seed.ts`, 3 das 9 linhas são DADO e não comentário, as descrições do array `BUDGET_LINES` que `prisma db seed` grava em `BudgetLine.description` ("Tecnologia <U+2014> Infraestrutura" virou "Tecnologia - Infraestrutura", idem BL-2026-002 e BL-2026-003), e aqui o travessão virou hífen simples. A mensagem afirma "zero occurrences" no repositório, o que é falso: sobraram 16 linhas em `.env.example`, `.gitignore`, uma migration e `public/brand/alai-concepts.html`. A varredura mira só o U+2014; o traço médio U+2013 sobrevive até hoje em `RequestActions.tsx:1712`, `dashboard-data.ts:380-383` e `pdf/pedidoCompra.tsx:26`.
- **eb2e650, Plano de correção derivado da auditoria.** Nasce `docs/plano-correcao.md` com 416 linhas, e com ele a pasta `docs/`. Nenhuma correção é aplicada: é uma lista de prompts, cada fase pensada para rodar em sessão separada. Registra os guardrails (nenhuma escrita no banco sem autorização na hora, motivada pelo apagamento completo do banco de desenvolvimento em 18/08 por causa nunca identificada; grafia "alAi"; proibição do travessão; baseline de 73/73 testes; commit e push por correção; parar o servidor antes de operação do Prisma por causa do EPERM na dll do query engine no Windows) e o estado do ambiente (banco vazio, 26 migrations reportadas como não aplicadas). O P0 nomeia arquivo e linha: `aprovacao/route.ts` PATCH por volta da linha 114, `middleware.ts:74`, `LOCAL_BYPASS_AUTH` em 12 a 18 pontos, `slack/events/route.ts` ~67. As fases têm mais itens do que a leitura resumida sugere: P1 tem seis itens (incluindo ausência de `error.tsx`, validação de env no boot e upload sem validação de tipo ou tamanho), P2 tem sete (incluindo estados vazios e jargão sem explicação), a seção de IA tem seis (incluindo sinalizações pedidas de volta ao modelo e registro de `usage` de tokens em `AiInsight`), e P3 traz ainda a revisão do uso de dois provedores de IA em paralelo. O problema anterior: a auditoria tinha achados espalhados, sem ordem de execução, e nada sobreviveria ao fim da sessão em que foi feita.
- **f9521eb, Regra do travessão passa a nomear o caractere por codepoint.** Uma linha em `docs/plano-correcao.md`: "Não use o caractere travessão (<U+2014>)" vira "Não use o caractere travessão (traço longo, U+2014)". O problema: eb2e650 escreveu o caractere literal dentro da própria regra que o proíbe, levando o repositório de 16 para 17 linhas com ele. Volta para 16. O título do commit fala em manter o grep "at zero", mas o repositório não estava em zero nem antes nem depois.

### Segurança crítica (P0)

- **1154a22, Só o aprovador designado pode registrar a decisão.** Duas guardas no PATCH de `src/app/api/requests/[id]/aprovacao/route.ts` (+22 linhas): validação de `decision`, devolvendo 422 para qualquer valor fora de APROVADO/REPROVADO (inclusive PENDENTE, que deixa de poder reabrir uma decisão), e um `else` no `if (personifiedBy)` chamando `requireRole(approval.approverId, ["APROVADOR"])` com `requireSelf` no padrão, devolvendo 403. O id passado é o do aprovador designado; quem compara sessão contra ator é o próprio `requireRole`. O problema anterior: a única checagem de identidade vivia dentro de `if (personifiedBy)`, então omitir esse campo pulava sessão, papel, comparação com `approval.approverId` e o teto de R$ 50 mil da personificação de uma vez, e como `personifiedBy` ficava null o histórico atribuía a decisão ao aprovador legítimo. Ressalva para quem ler depois: nesta data `requireRole` ainda lia `process.env.LOCAL_BYPASS_AUTH` direto, e é essa flag que desliga a comparação com a sessão; a correção só vale com a flag desligada, e ela só passou a ser ignorada em produção 48 minutos depois, em cfa074d. Colateral sem relação com segurança: o teste do PDF de pedido de compra ganhou timeout de 30000 ms.
- **4d31db4, Rotas de API passam a atravessar o middleware.** `src/middleware.ts` (+42/-1) acrescenta `/api/:path*` ao matcher e um bloco que exige `getToken()` para tudo sob `/api/`, devolvendo 401 em JSON em vez de redirect. Quatro prefixos ficam isentos porque têm autenticação própria: `/api/auth` (NextAuth), `/api/cron` (Bearer CRON_SECRET), `/api/erp` (Bearer ERP_API_KEY) e `/api/slack` (HMAC). Exige apenas sessão, não `canViewBoard`, para não travar quem só é Solicitante. O problema anterior: o matcher cobria só páginas, então baixar anexo, exportar a base em Excel e criar solicitação funcionavam sem autenticação. O número 57 de rotas confere (57 arquivos `route.ts`/`route.tsx` sob `src/app/api`); o "30 sem checagem própria" vem da mensagem do commit e não do diff, uma contagem conservadora dá 24. Dois detalhes: o bloco fica depois do early return de `LOCAL_BYPASS_AUTH`, e `/api/slack` foi isentado enquanto `verifySignature` ainda estava fail-open, combinação que só ficou segura 34 minutos depois, em c3d01a4.
- **cfa074d, Flag de bypass centralizada e neutralizada em produção.** Nasce `src/lib/bypass.ts` com `bypassAuthAtivo()`, que devolve false sempre que `NODE_ENV === "production"` independentemente do valor da variável, e `assertBypassNaoEstaEmProducao()`, chamada no escopo de módulo de `src/lib/auth.ts` e deliberadamente não no middleware, para não derrubar o runtime edge com erro ilegível. São funções e não constantes de módulo porque os testes precisam alternar a flag em runtime. Dezesseis arquivos trocam a leitura direta de `process.env` por `bypassAuthAtivo()`. Depois do commit, as únicas leituras reais de `process.env.LOCAL_BYPASS_AUTH` em código de aplicação são as duas dentro de `bypass.ts` (linhas 21 e 35); as outras 38 linhas em 24 arquivos são comentários, `.env.example`, `vitest.config.ts`, documentação e `vi.stubEnv` em testes. O problema anterior: a flag não desliga só o login, desliga a comparação sessão contra ator dentro de `requireRole` e abre as rotas administrativas, e não constava do `.env.example`. Entra lá agora, com valor "false" e bloco de advertência. Ressalva: a neutralização depende exclusivamente de `NODE_ENV === "production"`, então staging que não sete assim continua obedecendo a flag. Colateral: três dos 23 arquivos do commit (`.gitignore`, a migration `20260811182721_multi_approver_pools`, e `public/brand/alai-concepts.html` com 18 linhas de `<title>` e labels) são varredura de travessão, e a mensagem admite que "the earlier sweep filtered by extension and missed files that have none".
- **c3d01a4, Fecha o fail-open do Slack e autoriza a importação de contratos.** Em `src/app/api/slack/events/route.ts` (+7/-1, a linha trocada mais seis de comentário), `if (!secret) return true` vira `return false`. Em `src/app/api/contratos/import/route.ts` (+40), a importação passa a exigir sessão com papel ADMIN, com a checagem inteira dentro de `if (!bypassAuthAtivo())`, mais teto de 5 MB verificado em `file.size` antes da leitura e teto de 2000 linhas verificado depois do parse, ambos 413. A ordem importa: o teto de linhas roda depois de `XLSX.read` carregar a planilha na memória, então quem limita o pico de memória é o de tamanho. O problema anterior: a ausência de `SLACK_SIGNING_SECRET` transformava o webhook em porta aberta, com falha silenciosa, permitindo injetar mensagem no chat entre comprador e solicitante em nome de terceiros; e a importação em massa não checava nada, sendo que o e-mail do gestor vem da própria planilha, o que permitiria criar contratos falsos apontando para um executivo real, que entram no cron de renovação e disparam e-mail e Slack em nome do sistema.
- **f656e5d, Revoga acesso quando o usuário é desativado ou perde papel.** Em `src/lib/auth.ts`, o callback `jwt` inverte a condição: usuário inexistente ou inativo zera `userId`, `roles` e `canViewBoard` e marca `desativado = true`. O callback `session` passa a consultar o banco e deixa de cair no fallback `?? Boolean(token.canViewBoard)`, que era o valor velho do cookie. `maxAge` vai de 30 dias (padrão do NextAuth) para 8 horas. Em `src/middleware.ts`, quatro pontos trocam `!token` por `!token || token.desativado`. O problema anterior: `active` era checado apenas no callback `signIn`, então desativar alguém bloqueava logins novos mas não encerrava a sessão em curso, e um desligamento preservava acesso por semanas. Nuance que importa: a revogação é imediata só onde o banco é consultado, ou seja, no callback `session`. O middleware roda no edge e decide pelo cookie, e 23 das 57 rotas não citam `getServerSession`, `requireRole` nem `current-user`, então nelas o desativado só é barrado pelo middleware e o token carrega papéis antigos até a renovação; as 8 horas limitam essa janela, não a fecham. Além disso, o único ponto com `{ requireSelf: false }` (POST `/api/requests/[id]/aprovacao`, linha 70) não passa pela sessão e não sente a revogação imediata.

### Infraestrutura e etapas (P1, parte 1)

- **c532d09, Telas de erro, log estruturado e conferência de ambiente na inicialização.** Nascem `src/app/error.tsx`, `global-error.tsx` e `not-found.tsx`. Os dois primeiros mostram o `digest` do Next.js condicionalmente; o `not-found.tsx` não recebe prop de erro nenhuma e é só uma tela de 404 com dois botões. O `global-error.tsx` traz html/body próprios e estilo inline de propósito, porque cobre falha no layout raiz. Nasce `src/lib/logger.ts`, sem dependência externa: uma linha JSON por evento, com redação de campos sensíveis e desempacotamento de `Error` em `{nome, mensagem, stack}`, porque `JSON.stringify` achata Error para `{}`. Seis `.catch(() => {})` de servidor passam a registrar, e os dois crons passam a contar `falhasDeAviso`. Nenhuma das três telas usa o logger: `error.tsx` é client e registra com `console.error` num `useEffect`, com comentário dizendo que é intencional. Nasce `src/lib/segredos.ts` com `verificarTokenDeMaquina`, usando `crypto.timingSafeEqual` com checagem de comprimento antes, devolvendo 503 quando a variável não está configurada e 401 quando a credencial está errada; aplicado nos dois crons e em `src/lib/erpAuth.ts`. Nasce `src/lib/env.ts` com `validarAmbiente()`, chamada no topo de `src/lib/auth.ts`: 7 variáveis derrubam o boot em produção e 7 apenas avisam. O cron de alerta de contrato passa a gravar channel "EMAIL_E_SLACK". O problema anterior: nenhum `error.tsx` no projeto, 56 das 57 rotas sem try/catch, uma única chamada de console em todo o src/ e nenhum logger, e os tokens de máquina comparados como `auth !== \`Bearer ${process.env.CRON_SECRET}\``, que sem a variável vira a string "Bearer undefined", ou seja, ausência de configuração virando credencial válida. Alcance não afirmado na mensagem: `validarAmbiente()` só roda em quem importa `@/lib/auth`, e os dois crons e o middleware não importam. Arquivo tocado e não citado na mensagem: `src/app/layout.tsx`, só um comentário de quatro linhas sobre o `catch (e) {}` do script de tema.
- **2606a7b, Validação de anexo e fim do Pedido de Compra servido como arquivo estático.** Nasce `src/lib/upload.ts` com `validarAnexo()`: limite de 15MB, recusa de arquivo vazio e lista de permissão por extensão com 21 itens. A checagem é por extensão e não pelo MIME declarado, porque o MIME vem do cliente e é a extensão que determina como o arquivo é servido depois. Aplicada nas duas rotas de anexo, que também param de devolver `storageUrl`. Em `src/app/api/requests/[id]/pedido-compra/route.tsx` saem os imports de `fs/promises`, `path` e `renderToBuffer`: o POST deixa de gravar o PDF em `public/pedidos-compra/{código}.pdf` e o `pdfUrl` passa a apontar para a rota GET que regenera o documento do banco. O problema anterior: as rotas de anexo só checavam `file instanceof Blob`, enquanto a rota de foto de perfil já validava tamanho e tipo, e em produção o Vercel Blob grava com acesso público e serve com o Content-Type real, então um .html ou .svg anexado seria renderizado com script; e os PDFs iam para `public/` com nome previsível (PC-2026-0001.pdf, 0002...), permitindo enumerar fornecedor, CNPJ, valor negociado e condição de pagamento de toda a empresa, além de sumirem no deploy seguinte por ser disco efêmero. Duas precisões: a mensagem afirma que quatro PDFs órfãos foram movidos para fora de `public/`, o que não está no diff e não poderia estar, porque a pasta é gitignorada; e a rota GET não faz checagem de papel própria, então acabou a enumeração anônima, mas qualquer pessoa logada continua podendo baixar o Pedido de Compra de qualquer solicitação sabendo o id.
- **f25e745, 45 índices no banco e contador de código sequencial à prova de corrida.** `prisma/schema.prisma` ganha 45 `@@index` e a migration `20260819180000_indices_e_contador_de_codigo` tem 45 `CREATE INDEX` e 1 `CREATE TABLE`, nenhum DROP. Nasce o model `CodeCounter` e `src/lib/codigo.ts` com `proximoCodigo()`, que faz `INSERT ... ON CONFLICT ("prefix") DO UPDATE SET value = value + 1 RETURNING value` via `$queryRaw` parametrizado, substituindo o `count() + 1` em `src/app/api/requests/route.ts` e `src/app/api/tickets/route.ts`. O problema anterior: zero `@@index` em 835 linhas de schema (30 models, não 32 como diz a mensagem), e o PostgreSQL não indexa chave estrangeira sozinho, então toda busca era varredura sequencial, inclusive em `StageEvent`. O código sequencial tinha três defeitos ao mesmo tempo: corrida entre criações simultâneas gerando 500 pelo `@unique` sem try/catch, o ano era decorativo (o contador era sobre a tabela inteira), e apagar linha fazia o contador regredir. Buraco registrado: a migration não semeia o `CodeCounter` a partir dos códigos já emitidos, e nada mais no repositório faz isso, então num banco com PC-2026-0001..0500 o contador começa do zero e a primeira criação colide, exatamente a falha que o commit se propôs a eliminar. Banco novo não é afetado.
- **fe98882, Helper transacional de avanço de etapa, aplicado só na rota fiscal.** Nasce `src/lib/etapa.ts` com `avancarEtapa()`, que consulta `isValidTransition` do workflow.ts e devolve 422 quando o grafo não prevê a transição, e depois abre um `prisma.$transaction` onde o guard vira condição do próprio UPDATE (`updateMany` com `where {id, currentStage: de}`), lançando a sentinela `ConflitoDeEtapa` quando `count === 0` para devolver 409 em vez de 500; o `stageEvent.create` fica dentro da mesma transação. `notificarAvancoDeEtapa()` fica fora da transação de propósito. `src/app/api/requests/[id]/fiscal/route.ts` é a única rota reescrita, e o título do commit diz isso. `vitest.config.ts` sobe `testTimeout` para 15s. O problema anterior: as 16 rotas de etapa faziam ler, checar, escrever e registrar em passos soltos, com janela entre checar e escrever, um único `$transaction` em toda a base (`approval-levels/[level]/route.ts`), e `isValidTransition` só chamado pelos testes, ou seja, grafo decorativo em runtime. Detalhe não dito na mensagem: na rota fiscal o upsert do `FiscalDocument` continua antes do `avancarEtapa` e fora da transação dele, então num 409 o documento fiscal já foi gravado sem a etapa se mover.
- **9a1e74f, aguardando-entrega e tesouraria passam a usar o helper.** Commit puramente de migração, sem arquivo novo e sem teste novo. As duas rotas trocam a sequência solta por `avancarEtapa` e `notificarAvancoDeEtapa`, param de fazer `include: { requester: true }` no findUnique inicial (o helper já devolve) e ganham a mensagem de 409. Some o mapa local `STAGE_LABEL` de aguardando-entrega, que duplicava `STAGES` do workflow.ts, e o ternário de rótulo de tesouraria. Mesmo detalhe da rota fiscal: o upsert do `Payment` continua antes e fora da transação.

### Migração das rotas de etapa (P1, parte 2)

- **9f99b92, Triagem e Aprovação do Gestor passam pelo helper.** Em `aprovacao-gestor/route.ts` os cinco campos da decisão do gestor e, na reprovação, `status` CANCELADO mais `cancelReason` passam por `dadosExtras`, ou seja, dentro da transação. Em `triagem/route.ts` migram os dois caminhos, o atalho de CANCELAMENTO (TRIAGEM para JURIDICO) e o normal (TRIAGEM para VALIDACAO_ORCAMENTARIA, gravando `buyerId`, `needsContract`, `needsMapping`, `valueType`, `lane`, `estimatedValue` e `fragmentationFlag` na mesma transação). Em `src/lib/etapa.ts` o tipo de `dadosExtras` muda de `PurchaseRequestUpdateManyMutationInput` para `PurchaseRequestUncheckedUpdateManyInput`, para permitir gravar a chave estrangeira crua `buyerId`. Duas coisas que a mensagem não menciona: o texto do 409 foi reescrito nas duas rotas, e o corpo da resposta muda de forma, passando a embutir o objeto do solicitante. Ficam fora da transação a notificação de risco de fracionamento e o e-mail de reprovação. O problema anterior: uma falha entre a escrita da solicitação e a do evento deixava a Triagem já em Validação Orçamentária sem registro de como chegou lá. Nenhum teste adicionado, e a Triagem não tinha nem tem arquivo de teste próprio no repositório.
- **b3c2b88, Validação Orçamentária passa pelo helper.** Um arquivo (+40/-26). Os três caminhos que avançam etapa passam a usar `avancarEtapa`: orçamento disponível, exceção REPROVADA (com status e `cancelReason` na mesma transação) e exceção APROVADA. A criação da exceção orçamentária continua como escrita solta, fora da transação. O ternário local de rótulo some. Dois detalhes: o comentário do evento passou de `observation || undefined` para `observation || null`, gravando null explícito no lugar do default do Prisma; e o `requester` embutido na resposta vale para dois dos três caminhos, já que o de exceção reprovada devolve `{ status: "REPROVADO" }` literal, antes e depois. A assimetria de o caminho de exceção aprovada não avisar o solicitante foi mantida de propósito e virou comentário no código.
- **402c2c8, Commit que promete a migração de Aprovação e entrega só o import.** A mensagem descreve a migração completa dos dois caminhos da rota de Aprovação, o guard atômico contra dois aprovadores de Nível 2/3 decidindo ao mesmo tempo, e afirma que "todas as seis rotas com ramificação estão migradas". O diff real é um arquivo e uma linha: acrescenta `import { avancarEtapa } from '@/lib/etapa'` em `aprovacao/route.ts`, e o import fica sem uso. Nenhum comportamento muda. O problema que a mensagem descreve é real (dois aprovadores decidindo quase junto liam o mesmo lote, ambos avançavam a etapa, gerando evento duplicado e e-mail dobrado) e segue de pé até 2d2607f. A causa está registrada em 2d2607f, não no commit imediatamente seguinte.
- **cc9a6fb, Mapa de Cotação, Medição, Jurídico e Due Diligence migram.** Quatro arquivos, e aqui as edições realmente entraram no diff: `due-diligence/route.ts` (para COTACAO quando aprovado, para CANCELADO quando reprovado), `juridico/route.ts` (para CONCLUIDO quando `demandType` é CANCELAMENTO, senão para PEDIDO_COMPRA; some a variável `nextLabel` e o import do gmail), `mapa-cotacao/route.ts` (MAPA_COTACAO para APROVACAO) e `medicao/route.ts` (MEDICAO para FISCAL). Detalhe não registrado na mensagem: em `mapa-cotacao` as duas escritas de cotação (o `updateMany` zerando `selected` e o `update` marcando o vencedor) continuam fora da transação, então a seleção do fornecedor vencedor segue não atômica em relação ao avanço.
- **2d2607f, Fecha a migração e dá transação ao override.** Seis arquivos. `aprovacao/route.ts` finalmente migra os dois caminhos que 402c2c8 prometeu, com o comentário sobre a corrida entre aprovadores agora no código. Migram também `cotacao`, `mapeamento-contrato` (com o `prisma.contract.create` ficando antes e fora da transação, então o contrato pode nascer sem a solicitação avançar para CONCLUIDO) e `pedido-compra` (com a criação do purchaseOrder e o update do `pdfUrl` fora da transação). `stage-override/route.ts` deliberadamente não usa `avancarEtapa`, porque existe justamente para contornar o grafo, e ganha um `$transaction` próprio com `updateMany` condicionado, `ConflitoDeEtapa` como sentinela e 409; para isso a classe deixa de ser privada e passa a ser exportada de `src/lib/etapa.ts`. As mensagens de 409 do helper e do override são textos diferentes, com a frase de fecho em comum ("Recarregue a página para ver o estado atual"). Duas imprecisões na mensagem do commit: ela lista como cobertas aqui as quatro rotas que foram feitas em cc9a6fb, e diz que o script deixou "duas rotas intocadas" quando só uma (aprovacao) tinha sido prometida e não entregue. Estado final: 15 rotas de etapa cobertas, 14 chamando `avancarEtapa` mais o `stage-override` com transação própria; as chamadas soltas a `stageEvent.create` que sobram estão apenas em `src/app/api/requests/route.ts` linhas 114 e 143, que é criação de solicitação, não rota de etapa. Nenhum dos cinco commits deste bloco criou teste algum: a garantia se apoia em `src/lib/etapa.test.ts` (119 linhas), criado em fe98882, que testa o helper e não as rotas, e a verificação usada foi grep por `stageEvent.create` solta.

### Clareza para quem usa (P2)

- **e1c0610, Rótulos legíveis no lugar dos valores de enum.** Nasce `src/lib/rotulos.ts` (60 linhas) reunindo `PRIORITY_LABEL`, `DEMAND_TYPE_LABEL` e `STATUS_LABEL` (movidos de `dashboard-data.ts`) e criando `CONTRACT_STATUS_LABEL` e `PAYMENT_STATUS_LABEL`, mais o helper `rotulo(mapa, valor)`, que devolve o valor cru quando não há tradução, decisão documentada no arquivo. Aplicado em 13 pontos de renderização. O problema anterior: `PRIORITY_LABEL` existia e nunca era usado, então em cinco telas a pessoa lia CRITICA, MEDIA e BAIXA sem acento e em caixa alta; `DEMAND_TYPE_LABEL` estava duplicado em dois arquivos; status de contrato e de pagamento não tinham mapa. Três ressalvas de arquivo: a mensagem afirma que nenhum enum aparece como texto visível em tela nenhuma, mas o painel de fallback de `RequestActions.tsx` continuava imprimindo o código cru da etapa até b384aec; `TICKET_STATUS_LABEL` não foi movido, segue em `src/lib/tickets.ts`; e o re-export deixado em `dashboard-data.ts` não tem nenhum consumidor. **Duas regressões, ambas ainda presentes no HEAD.** Primeira: em `src/app/chamados/[category]/[id]/page.tsx` a prop passou a ser o rótulo, mas `ChamadoThread.tsx` usa esse valor como `value` de um `<select>` cujas `<option>` têm a chave crua, então nenhuma opção casa e o dropdown de status do chamado exibe sempre a primeira opção, independentemente do status real (a gravação segue correta, porque o `onChange` manda `e.target.value`). Segunda, mais grave: em `src/app/contratos/[id]/page.tsx:164` a prop `status` de `ContractActions` passou a receber "Cancelado" em vez de "CANCELADO", e `ContractActions.tsx:15` compara `status === "CANCELADO"` como lógica, não como texto, então um contrato já cancelado passa a exibir o formulário "Cancelar contrato" em vez do aviso; e `src/app/api/contracts/[id]/route.ts` não tem guarda de status, aceita `action: "CANCELAR"` de novo, reenvia e-mail e Slack para todos os usuários TESOURARIA e regrava o status.
- **3e2262f, Estados vazios que apontam para a ação que os resolve.** Reescreve 13 estados vazios. Doze são troca de uma linha de texto; o estrutural é `src/app/solicitacoes/minhas/page.tsx`, que passa a ter dois parágrafos mais um link `Abrir minha primeira solicitação`. Quatro listas filtradas trocam "neste recorte" por instrução de limpar filtros, o `MultiUserPicker` explica que um administrador concede papel em Administração aba Acessos, e assim por diante. O problema anterior: de aproximadamente 25 estados vazios, só 3 sugeriam um próximo passo, sendo o pior a tela principal do solicitante, com o botão de abrir logo acima e sem menção nenhuma. Ressalvas: a mensagem fala em 25 estados e o diff mexe em 13; os painéis de dashboard ficaram de fora por decisão explícita, mas isso não cobre a diferença. O novo `emptyLabel` padrão de `AttachmentsPanel` alcança só 1 dos 3 usos, porque `contratos/[id]:101` e `solicitacoes/[id]:211` passam valor próprio.
- **493e943, Mensagens de erro sem identificador de código.** Acrescenta 62 linhas a `src/lib/rotulos.ts`: o mapa `CAMPO_LABEL` com 42 chaves e o helper `campo(chave)`. Reescreve 27 mensagens em 23 arquivos de rota, em três padrões: interpolação da chave do objeto (`Campo obrigatório ausente: ${key}`) vira frase com o rótulo do formulário, com final próprio por rota ("antes de enviar a solicitação", "para gerar o Pedido de Compra", "para cadastrar o contrato"); mensagens que expunham tipo de linguagem ou nome em inglês passam a dizer o que fazer; e código de etapa cru sai via `STAGES[...].label`, então o erro do ERP lê "está em Aguardando Entrega/Conclusão" em vez de AGUARDANDO_ENTREGA. O problema anterior: de aproximadamente 120 strings de erro, cerca de 40 continham identificador em inglês, enum cru ou tipo de linguagem, e só cerca de 15 diziam o que fazer. Um teste existente foi atualizado: em `pedido-compra/route.test.tsx`, `toMatch(/contactPhone/)` virou `toMatch(/Telefone do contato/)` mais `not.toMatch(/contactPhone/)`, ou seja, a propriedade travada deixou de ser o comportamento antigo e passou a ser a garantia de não vazamento.
- **7b5fc1c, Vocabulário fechado: uma palavra por conceito.** Seis arquivos, cerca de 15 trocas de texto, nenhuma mudança de lógica. Chamado deixa de se chamar solicitação em `NdaRequestForm.tsx` (7 pontos) e `ChamadoRequestForm.tsx`; Gestor e Aprovador viram papéis distintos na escrita ("Por Aprovador" vira "Por Gestor", "Gestor aprovador" vira "Gestor do centro de custo"); e o rótulo do `NovaSolicitacaoForm` ganha concordância. O problema anterior: o formulário de NDA chamava o objeto de "solicitação" enquanto o título da página dizia "Novo Chamado", e Gestor e Aprovador são dois papéis reais (o gestor do centro de custo decide na etapa 2, o aprovador de alçada na etapa 8) que carregam o mesmo papel APROVADOR no banco. Defeito deixado pelo próprio commit: em `NdaRequestForm.tsx:98` a troca de "Sua solicitação" para "Seu chamado" não acompanhou o particípio da linha seguinte, e a tela de sucesso lê "Seu chamado {code} foi registrada".
- **b7754f8, Retorno visível nas ações dos painéis administrativos.** Nasce `src/components/useAcaoRemota.tsx` (85 linhas) com o tipo `EstadoDaAcao`, o hook `useAcaoRemota()` e o componente `StatusDaAcao`. No caminho de falha o hook lê o corpo com `res.json().catch(() => null)`, para resposta não JSON não derrubar o tratamento do erro; exceção de rede vira "Sem conexão com o servidor. Verifique sua rede e tente de novo."; o sucesso pisca "Salvo" e volta a ocioso em 2500 ms. `StatusDaAcao` usa `role="status"` no sucesso e `role="alert"` no erro. Seis componentes migram, três deles revertendo o valor anterior quando a chamada falha, e `RoleAccessToggles` deixa de aplicar otimista. O problema anterior: os painéis rodavam `if (res.ok) router.refresh();` sem else, então conceder papel, desativar usuário e trocar aprovador de alçada eram silenciosos no sucesso e na falha; nos pickers era pior, porque atualizavam a tela antes da resposta e na falha a tela afirmava algo falso sobre quem aprova o quê.
- **b384aec, Confirmação do que aconteceu após cada ação de etapa.** Um arquivo, `src/components/RequestActions.tsx` (+92/-5). O switch de etapas vira uma IIFE e o componente passa a retornar `<SuccessBox>` mais o formulário. A função `call()` guarda `request.currentStage` antes do fetch e chama `mensagemDeSucesso(etapaAnterior, data)`, que lê a forma da resposta e escolhe a frase: `waitingForOtherApprovers`, REPROVADO ou CANCELADO, DOCUMENTO_REPROVADO, PAGAMENTO_PROGRAMADO, EXCECAO_PENDENTE, DEVOLVIDO, MINUTA_EM_ANDAMENTO, ou o avanço genérico com o rótulo da nova etapa. As formas existem nas rotas (`aprovacao:225`, `fiscal:40`, `tesouraria:47`, `validacao-orcamentaria:72`, `triagem:62`, `juridico:35`). O `SuccessBox` vive no pai porque depois do `router.refresh()` o formulário da etapa anterior desmonta. Este commit conserta o que e1c0610 dera por concluído: o painel de fallback deixa de imprimir o código cru da etapa, e a própria mensagem registra "missed by the earlier label pass". O problema anterior: nenhuma das 14 ações de etapa dava retorno, chamavam `router.refresh()` e nada mais, então quem aprovava meio milhão de reais via o formulário sumir e tinha que deduzir que funcionou.
- **0d36474, CNPJ normalizado e importação de contratos sem duplicar.** Nasce `src/lib/cnpj.ts` (37 linhas) com `normalizarCnpj` (só os dígitos, null quando não sobra nada) e `formatarCnpj`. A normalização é aplicada em quatro pontos de escrita e leitura: `contratos/import`, `mapeamento-contrato`, `pedido-compra` (com uma guarda redundante em runtime, necessária para o TypeScript estreitar o tipo) e `supplier-history`. A importação ganha, antes do create, um `findFirst` por `supplierName` + `startDate` + `endDate` (+ `supplierCnpj` quando houver), e a linha repetida vira erro com "já está cadastrado. Linha ignorada para não duplicar"; o comentário registra por que a checagem ficou na aplicação e não numa constraint (o `supplierCnpj` é anulável e o Postgres trata nulos como distintos, então a constraint deixaria passar justamente as linhas sem CNPJ). O problema anterior: o CNPJ era texto livre, gravado como chegava, e o `@unique` em `Supplier.cnpj` não protege porque para o banco são strings diferentes; o estrago não é cosmético, o controle anti-fracionamento soma os Pedidos de Compra comparando CNPJ por igualdade exata, então com formatos divergentes a soma dá zero e o risco nunca é sinalizado, ou seja, o antifraude parava de funcionar em silêncio. Na importação, subir a mesma planilha duas vezes duplicava a carteira inteira, o que faz o cron de renovação mandar dois avisos por contrato e o dashboard contar em dobro. Duas ressalvas: não há migração de backfill, então os CNPJs já gravados formatados continuam como estão, e o único ponto que compensa isso na leitura é `supplier-history`; e `formatarCnpj` não tem nenhum consumidor no commit nem no HEAD, então a decisão registrada no cabeçalho do arquivo (guardar só os dígitos e formatar na exibição) ficou pela metade, e o CNPJ passa a aparecer sem formatação em `contratos/page.tsx:103`, `contratos/[id]/page.tsx:118`, `StageHistoryPanel.tsx:256` e `SupplierPicker.tsx:52`.
- **9d26c38, Explicação de cada etapa dentro do painel da etapa.** Um arquivo (+59, nenhuma remoção). Entra o mapa `EXPLICACAO_DA_ETAPA` com 15 entradas, chaveado pelo título do painel e não pelo enum, e o componente `Panel` passa a renderizar a frase da etapa mais o link "Ver o manual do processo" apontando para `/api/manual/pdf`. As 15 chaves casam com os 16 usos de `<Panel title=...>` ("Triagem" aparece duas vezes). O problema anterior: o manual completo existia mas só era alcançável pelo rodapé da home, e nenhum dos 16 painéis apontava para ele. Duas observações: a chave "Triagem" casa com o título do painel mas o rótulo dessa etapa em `workflow.ts` é "Homologação e Triagem", e "Validação orçamentária" está com "o" minúsculo no mapa contra "Validação Orçamentária" em `workflow.ts`; e a mensagem lista Personificação entre os casos sem ajuda, mas Personificação não é etapa nem título de painel e continua sem explicação depois do commit.

### Constraints, auditoria e achados soltos (P3)

Nove commits entre 19h12 e 20h10, na ordem 405268f, 8aacf1a, e8e1375, 8e420c3, 0382a3d, ca1e8aa, 7493bd7, f55b66d, 2faf6f1. Três migrations aditivas nascem aqui.

- **405268f, Documento de auditoria ganha coluna de situação por achado.** Só `src/lib/pdf/auditoriaSistema.tsx` (+71/-9), conteúdo de PDF. Cria a constante `SITUACAO` e estende o componente `Finding` com `situacao` (default "aberto") e `comoFoiResolvido`, que renderiza uma linha verde prefixada por "Correção:". Marca quatro dos 16 achados: n=2 e n=11 como RESOLVIDO, n=14 e n=15 como PARCIAL. Acrescenta um card "O que foi corrigido além desta lista", resumindo o trabalho vindo da revisão de segurança paralela. O problema anterior: o documento listava os 16 achados como se todos estivessem abertos, e o componente `Finding` nem tinha campo para situação, o que num artefato de auditoria é pior que estar desatualizado. **Divergência registrada:** como este é o primeiro commit do bloco, o documento nasce desatualizado em relação ao próprio bloco, e nenhum commit posterior reabriu o arquivo. Os commits seguintes fecham mais nove achados que continuam marcados EM ABERTO no PDF: n=3 (ca1e8aa), n=5, n=6, n=7 e n=9 (f55b66d), n=8 (7493bd7), n=10 (f55b66d), n=12 (2faf6f1) e n=13 (0382a3d). Ao fim do bloco eram 4 achados marcados contra 13 de fato atendidos. No HEAD, seis carregam situação (n=1, n=2 e n=11 como RESOLVIDO, n=4, n=14 e n=15 como PARCIAL), por causa de 4ff4047, 4a4906a e bce687f.
- **8aacf1a, Trilha append-only de mudança de permissão.** Migration `20260819200000_auditoria_de_permissao` cria a tabela `PermissionChange` com `targetUserId`, `actorId`/`actorEmail` nuláveis, `kind`, `antes`, `depois` e `createdAt`, dois índices e FK para User com `ON DELETE RESTRICT`, deliberada para impedir que apagar um usuário apague a trilha dele. Nasce `src/lib/auditoria-permissao.ts` com `registrarMudancaDePermissao()` e `comoTexto()`. Três pontos de gravação: desativação e troca de papéis em `users/[id]/route.ts`, e troca de aprovadores de alçada em `approval-levels/[level]/route.ts`, com um registro por pessoa afetada, tanto quem saiu quanto quem entrou. O estado anterior é lido antes do `deleteMany`, único momento em que ainda existe. A gravação é best-effort dentro de try/catch, e a falha vai para `logger.error`; mudança que não muda nada não é registrada. O problema anterior: `UserRole` e `ApprovalLevelApprover` eram apagados e reescritos sem rastro, e são exatamente as duas tabelas que decidem quem aprova dinheiro. Três pontos para o arquivo: o tipo `TipoDeMudanca` declara `GESTOR_DE_CENTRO_DE_CUSTO` sem nenhum call site, então trocas de gestor de centro de custo não entram na trilha; a trilha é write-only, nenhuma tela nem rota lê `PermissionChange`, as únicas leituras estão no arquivo de teste; e o doc comment do `PermissionChange` foi colado no fim do doc comment do `CodeCounter` sem linha em branco, então o `CodeCounter` ficou sem doc comment próprio para o parser.
- **e8e1375, Onze CHECK constraints no banco, uma delas quebra a rota de confirmação do ERP.** Migration `20260819210000_constraints_de_coerencia`, escrita à mão porque o Prisma não modela CHECK: `PurchaseRequest.status`, `PurchaseRequest.managerApprovalDecision`, coerência entre decisão e data da decisão do gestor, `Contract.status`, `Payment.status`, `Notification.channel`, `Notification.status`, `ContractAlert.channel`, `RequestChatMessage.authorRole`, `RequestChatMessage.source` e `SimpleTicket.requestKind`. `SimpleTicket.status` e `Approval.decision` ficam de fora porque já são enum no Postgres. O comentário avisa que as constraints são invisíveis ao schema.prisma e que um `migrate dev` futuro pode acusar drift. O problema anterior: vários campos que são enum na prática eram texto livre, com os valores válidos escritos só num comentário do schema, e a validação vivia só na aplicação. **Defeito introduzido:** a constraint `Notification_canal_valido` aceita apenas ('EMAIL','SLACK'), transcrita do comentário `// EMAIL | SLACK` do model, que já estava errado. `src/app/api/erp/purchase-requests/[id]/confirm/route.ts:55` faz `notification.create` com `channel: "ERP"`, e o create está fora de try/catch, então a rota de confirmação do ERP passa a devolver erro, com o `erpSyncedAt` já gravado antes, ou seja, a solicitação fica marcada como sincronizada com a chamada falhando. A constraint codificou fielmente um comentário desatualizado em vez do comportamento real do código. Cobertura de teste parcial: 4 das 11 constraints são testadas.
- **8e420c3, Três correções pontuais no manual do processo.** Só `src/lib/pdf/manualProcesso.tsx` (+17/-3). O trecho sobre alçadas deixa de dizer que "qualquer uma delas pode decidir" e passa a descrever que cada designado decide a própria aprovação, que a personificação é a exceção controlada e que nos Níveis 2 e 3 as duas pessoas precisam decidir enquanto uma única reprovação já cancela. O bullet de anexos ganha os tipos aceitos e o limite de 15MB. Card novo "A sessão expira em 8 horas". Os dois números conferem contra o código (`upload.ts` e `auth.ts:138`). O problema anterior: o manual descrevia um sistema que não existia mais em três pontos, sendo o de alçadas o grave, porque ensinava o comportamento que a correção de autorização já tinha eliminado, e o de anexos virou urgente porque os uploads passaram a ser recusados sem que a pessoa tivesse como saber por quê. Ressalva: a última frase do card afirma que toda alteração de acesso fica registrada, o que não vale para troca de gestor de centro de custo, que 8aacf1a deixou sem call site.
- **0382a3d, Os seis papéis de etapa passam a ser concedíveis pela tela de Acessos.** `src/lib/roles.ts` ganha `ACCESS_ROLES` (5), `STAGE_ROLES` (6, cada um com um campo `etapa` que vira tooltip) e `ALL_MANAGED_ROLES`. Em `users/[id]/route.ts` a constante local `MANAGED_ROLES` passa a ser `ALL_MANAGED_ROLES`, e é essa linha que faz a mudança valer, já que ela governa a validação do corpo e o escopo do `deleteMany`. `RoleAccessToggles.tsx` passa a renderizar duas linhas rotuladas "Acesso" e "Etapa", e `/admin/acessos` perde a coluna somente-leitura "Outros papéis (etapa)". O problema anterior: Jurídico, Privacidade, Fiscal, Tesouraria, Coordenação e Gerente F&NC apareciam como texto sem edição, e a única forma de tornar alguém Fiscal ou Tesouraria era mexer direto no banco, sendo justamente os papéis que decidem sobre nota fiscal, pagamento, contrato e exceção orçamentária. Consequência a saber: como `MANAGED_ROLES` agora abrange os onze, o `deleteMany` do PATCH apaga todos os papéis antes de recriar, e qualquer chamador que envie só os cinco de acesso passa a revogar silenciosamente os de etapa; na prática o único chamador reenvia o conjunto inteiro. Resíduo de edição: três linhas em branco onde estavam as constantes em `src/app/admin/acessos/page.tsx`.
- **ca1e8aa, O alerta de fracionamento passa a ser enviado, não só registrado.** Em `triagem/route.ts`, o bloco `if (fragmentation.flagged)` deixa de criar à mão uma linha de `Notification` com status "ENVIADO" e passa a chamar `sendPurchaseEmail` com o template novo `templates.riscoFracionamento` (que nasce em `src/lib/integrations/gmail.ts`, arquivo modificado pelo commit) e `sendSlackDM`. O template nomeia a alçada em que a solicitação cai sozinha, a alçada que ela alcança somando os últimos 12 meses do fornecedor, e diz explicitamente que isso não bloqueia a solicitação. O registro manual sumiu porque `sendPurchaseEmail` já grava o `Notification` com ENVIADO ou FALHA conforme o resultado real. Nasce `src/lib/destinatarios.ts` com `DESTINO_CONTROLADORIA`, lido de `EMAIL_CONTROLADORIA` com padrão `controladoria@acerto.com.br`, e o cron de escalonamento troca seu endereço fixo por essa constante. O problema anterior: a Triagem detectava o risco, gravava uma linha afirmando que a Controladoria tinha sido notificada e não mandava nada, o que é pior que não avisar, porque nem lendo o log dava para perceber a falta. Passou a importar mais porque a normalização de CNPJ (0d36474) fez a detecção voltar a funcionar.
- **7493bd7, extraBudget passa a ser gravado, e o comprovante do FP&A vira exigência de API.** Migration `20260819220000_persistir_orcamento_extra` adiciona `PurchaseRequest.extraBudget BOOLEAN NOT NULL DEFAULT false`. Em `src/app/api/requests/route.ts` o POST passa a incluir `extraBudget: Boolean(extraBudget)` no create: o campo já era desestruturado (linha 31) e já era usado numa validação (linha 67), mas nunca chegava a coluna nenhuma. Em `validacao-orcamentaria/route.ts`, dentro do ramo que cria a exceção, entra uma guarda que responde 422 quando `request.extraBudget` é verdadeiro e não há anexo da categoria `APROVACAO_EXTRA_ORCAMENTARIA`. O problema anterior: uma solicitação aberta como Orçamento Extra terminava sem linha de orçamento e sem nenhum registro de que era extra-orçamentária, e o documento de apoio era exigido só pelo formulário, então uma chamada direta produzia exceção sem nada que a sustentasse. A checagem ficou na Validação Orçamentária e não na criação porque o anexo só pode ser enviado depois que a solicitação existe, já que a rota de upload precisa do id dela. Escopo real: a guarda está dentro do ramo alcançado quando `budgetOk` é false, então uma solicitação de Orçamento Extra concluída com `budgetOk` true segue sem verificação do comprovante; e nenhuma tela lê `request.extraBudget`.
- **f55b66d, Cinco divergências entre o que o código dizia e o que fazia.** Só uma muda comportamento: nasce `somarDiasUteis()` em `src/lib/workflow.ts`, que pula sábado e domingo, e a criação de `Approval` troca `dueAt.setDate(dueAt.getDate() + N)` por ela; o JSDoc registra que feriado não é considerado, por falta de tabela de feriados. As outras quatro são texto ou aviso: a mensagem de lembrete do cron de escalonamento lia `APPROVAL_ESCALATION_DAYS` enquanto o prazo saía de `APPROVAL_ESCALATION_BUSINESS_DAYS`, e agora as duas coisas vêm da mesma constante (o prazo nunca foi configurável, já era literal 3; o que o commit fez foi remover o último uso de `APPROVAL_ESCALATION_DAYS`, deixando-a órfã, e com isso tirar da mensagem a possibilidade de mentir); dois comentários do schema param de descrever a alçada de nível 3 como envolvendo CEO; em `aprovacao-gestor/route.ts` entra um `else` chamando `notificarAvancoDeEtapa`, porque a etapa avisava só na reprovação e era a única do fluxo assim; e o comentário do cron de contract-alerts para de afirmar cadência de segunda-feira, acrescentando que chamar duas vezes no mesmo dia reenvia, porque não há guarda de repetição. O problema anterior: nenhuma das cinco quebrava nada, cada uma enganava quem lesse o código, e o prazo de escalonamento se chamava dias úteis e contava dias corridos, então uma aprovação aberta numa quinta vencia no domingo. **Divergência registrada:** a mensagem afirma que sobre o CEO "a única menção sobrevivente é a que diz que não há CEO", o que é falso: `src/lib/workflow.ts:242` ainda traz `return 3; // Gerente F&NC + CEO`, dentro da própria função `approvalLevel()` e a vinte linhas do comentário que diz "sem CEO". O achado n=7 do PDF descreve o problema como sendo "comentários do schema", então nem o documento aponta para essa ocorrência. Segunda observação: não há migration de backfill, então as Approvals existentes seguem com `dueAt` em dias corridos.
- **2faf6f1, O link do e-mail de renovação passa a de fato pré-preencher o formulário.** `src/app/solicitacoes/nova/page.tsx` passa a receber `searchParams` e, havendo `origemContrato`, busca o contrato e repassa `contratoDeOrigem` ao formulário, que usa os dados como valor inicial: fornecedor, `demandType` fixado em RENOVACAO_CONTRATO, diretoria do contrato (com TECNOLOGIA como fallback), descrição curta montada e descrição longa com a data de fim de vigência. O centro de custo é resolvido dentro do useEffect que carrega `/api/cost-centers`, casando por nome normalizado, porque o `Contract` guarda `costCenter` como texto livre; não achando, deixa em branco em vez de chutar errado numa solicitação que define quem aprova. Entra um aviso com `role="status"` dizendo de onde vieram os dados e pedindo conferência. O problema anterior: o alerta de renovação mandava as pessoas para `/solicitacoes/nova?origemContrato=<id>` prometendo formulário preenchido, e o parâmetro não era lido em lugar nenhum. Resíduo: o select do servidor pede `supplierCnpj`, mas o campo não é repassado e o formulário não tem campo de CNPJ, então fica uma leitura morta e a solicitação de renovação criada por esse caminho nasce sem o CNPJ do fornecedor, que é justamente a chave do controle anti-fracionamento corrigido em ca1e8aa.

### Decisões de negócio e mapeamento

- **4ff4047, Triagem de fornecedor sinaliza na tela, e o upload de anexo para de estourar 500.** Três coisas independentes. Em `src/app/api/requests/[id]/attachments/route.ts`, o `import type` vira import de valor e a rota passa a conferir `stageInformada in Stage` e `categoriaInformada in AttachmentCategory` antes de gravar, caindo em `request.currentStage` e GERAL quando o valor é desconhecido. `prisma/schema.prisma` ganha `TRIAGEM_FORNECEDOR` no enum `AttachmentCategory`, com migração aditiva; em `solicitacoes/[id]/page.tsx` os anexos são separados em dois grupos, e o painel de triagem é renderizado antes do painel geral, aparecendo quando já existe evidência ou quando a etapa está em `ETAPAS_COM_FORNECEDOR_DEFINIDO` (de COTACAO em diante, incluindo CONCLUIDO mas não CANCELADO). Em `RequestActions.tsx` o `PedidoCompraForm` guarda o `screeningStatus` do fornecedor e renderiza um `WarningNotice` para PENDENTE, que diz explicitamente que não impede emitir o Pedido de Compra, e outro para REPROVADO, que diz apenas "Confirme com Compras antes de emitir o Pedido de Compra", sem afirmar que não bloqueia. O problema anterior: `stage` e `category` vinham do formulário e eram convertidos direto para o enum do Prisma com cast, então qualquer texto fora da lista quebrava no banco e voltava como 500; e a triagem básica de fornecedor era regra escrita e testada mas nunca aplicada, sem lugar para guardar a evidência da verificação, que o comprador faz fora do sistema. A decisão de negócio registrada é que a triagem informa e não bloqueia. **Registro importante:** o commit não liga a função que existia para isso. `requiresBasicVendorScreening` (em `src/lib/workflow.ts:344` neste commit, 391 no HEAD depois dos dois commits seguintes) continua sem nenhum chamador em rota nenhuma, referenciada só pelo próprio teste. Foi criado um caminho paralelo de sinalização na UI em vez de ativar a regra existente, e o texto do achado na auditoria continua dizendo, corretamente, que a função é código morto.
- **4a4906a, Os tempos por etapa saem do código e viram texto na tela, como referência.** Nasce `expectativaDaEtapa(stage, diretoria)` em `src/lib/workflow.ts`: devolve `slaDaysTecnologiaRevenue` para TECNOLOGIA e REVENUE, `slaDaysCorporativo` no resto (inclusive com diretoria null, escolha conservadora comentada no código) e null quando a etapa não tem número. Em `solicitacoes/[id]/page.tsx` entra uma linha com o tempo esperado da etapa atual, a previsão de conclusão (`request.slaDeadline`, que já era calculada na criação) e o aviso de que são tempos de referência. No manual, "Prazo esperado" vira "Tempo esperado" e a seção "Prazos e escalonamento" é reescrita em três parágrafos. O problema anterior: `slaDaysCorporativo` e `slaDaysTecnologiaRevenue` existiam em STAGES desde o início e nenhuma linha de código os lia, eram documentação escondida num arquivo TypeScript; a previsão de conclusão só aparecia nos painéis gerenciais; e o manual afirmava que cada etapa tem um prazo esperado, o que era falso. Detalhes que o commit não conta e que bce687f corrigiu pela metade: o `<p>` passou a ser renderizado sempre, gerando parágrafo vazio quando não havia nem tempo nem previsão, e o texto dizia "dia/dias" enquanto o manual falava em dias úteis; ambos corrigidos. O terceiro ponto não foi corrigido: `expectativaDaEtapa` continua devolvendo o número cru de STAGES, sem nenhuma conta de dia útil, em bce687f e no HEAD, ou seja, a unidade foi resolvida rotulando o número, não calculando-o. Além disso, o corpo do achado 1 da auditoria continua escrito "só existem em 8 das 16 etapas e nunca são lidos em código"; só os atributos `situacao`/`comoFoiResolvido` foram acrescentados.
- **bce687f, Preenche os tempos das seis etapas de execução e acerta a linha na tela.** MAPA_COTACAO 1/2, PEDIDO_COMPRA 1/1, MEDICAO 2/2, FISCAL 2/2, TESOURARIA 3/3 e MAPEAMENTO_CONTRATO 2/2 (Corporativo / Tecnologia e Revenue), passando de 7 para 13 etapas com tempo. SOLICITACAO e AGUARDANDO_ENTREGA ganham comentário explicando que a ausência é proposital, a primeira por ser o próprio formulário e a segunda por depender do prazo combinado com o fornecedor. O comentário do `StageDefinition` passa a registrar a procedência: as etapas até Jurídico vêm do desenho original do fluxo, as de Mapa de Cotação em diante foram estimadas com o time em ago/2026 sem medição de histórico, para revisar quando houver dado real de duração via `StageEvent`. O parágrafo da tela vai para dentro do guard, acabando com o parágrafo vazio, e o texto vira "dia útil"/"dias úteis". No manual os mesmos seis valores entram nas etapas 7, 10, 12, 13, 14 e 15. O achado 1 da auditoria passa de "parcial" para "resolvido", mas o corpo do achado continua sendo a frase original, agora errada nas duas metades.
- **2c04fcd, Documento de plano para autorização nas rotas de leitura: só o plano.** O commit inteiro é `docs/plano-autorizacao-leitura.md`, 99 linhas. Nenhuma linha de código, nenhum teste, nenhuma migração. Duas partes: uma tabela com seis achados vindos de um mapeamento multi-agente de seis subsistemas (31 suspeitas confirmadas por revisor cético, e os seis da tabela foram além disso confirmados manualmente no código antes de virar plano) e um prompt de execução pronto para colar numa sessão nova. Os seis achados: `GET /api/attachments/[id]/file` baixa qualquer anexo para qualquer sessão; `GET /api/contracts` devolve a carteira inteira mais o User completo do gestor; `GET /api/requests/[id]/ai-insight` usa `include: { requestedBy: true }` e joga `anthropicApiKey` e `geminiApiKey` no JSON; `GET /api/tickets` e `/api/tickets/[id]` não aplicam o recorte por e-mail que as telas aplicam; `POST /api/tickets` aceita nome e e-mail pelo corpo, permitindo abrir chamado em nome de outra pessoa; e `PUT /api/approval-levels/[level]` colapsa aprovações pendentes distintas na mesma pessoa. A Etapa 2 do prompt traz uma sétima frente que não está na tabela: o chat da solicitação, em que qualquer autenticado lê a negociação e escreve como COMPRADOR com o nome que quiser, espelhando em DM no Slack. Dois achados foram reconferidos no código e procedem: a rota de download faz `findUnique` e devolve os bytes sem checagem de dono ou papel, e a de ai-insight faz o `include` de User em duas linhas (42 e 203), cujo modelo guarda as duas chaves em colunas de texto (`prisma/schema.prisma:73-74`). O prompt carrega as regras operacionais da base, entre elas a proibição explícita de editar arquivo com script Node usando template literal, porque "nesta base isso já falhou em silêncio várias vezes: o script imprime sucesso e não altera nada". O problema anterior: a auditoria fechou os caminhos de escrita e não tocou nos de leitura; o `src/middleware.ts` garante apenas que existe sessão em `/api/*` e diz por escrito que autorização por papel é responsabilidade de cada rota, mas as rotas de GET nunca implementaram a sua parte. Este é o último commit da linha, nada do plano foi implementado.

## Arquivos que nasceram nesta sessão

Documentação e plano:

- `docs/plano-correcao.md` (eb2e650), 416 linhas: lista de prompts por fase (0, P0 a P3 e correções de IA) com guardrails e estado do ambiente.
- `docs/plano-autorizacao-leitura.md` (2c04fcd), 99 linhas: tabela de brechas de autorização em rotas de leitura mais prompt de execução. Não implementado.

Segurança:

- `src/lib/bypass.ts` (cfa074d): `bypassAuthAtivo()` e `assertBypassNaoEstaEmProducao()`, ponto único de leitura de `LOCAL_BYPASS_AUTH`.
- `src/lib/segredos.ts` (c532d09): `verificarTokenDeMaquina()` com `timingSafeEqual`, 503 sem configuração e 401 com credencial errada.
- `src/lib/env.ts` (c532d09): `validarAmbiente()`, 7 variáveis obrigatórias em produção e 7 com aviso.
- `src/middleware.test.ts` (4d31db4): 9 testes, 6 de rotas de API e 3 de página.
- `src/lib/bypass.test.ts` (cfa074d), `src/lib/auth.test.ts` (f656e5d), `src/lib/segredos.test.ts` e `src/lib/env.test.ts` (c532d09).
- `src/app/api/requests/[id]/aprovacao/route.auth.test.ts` (1154a22): 4 testes de autorização com a flag desligada via `vi.stubEnv`.
- `src/app/api/requests/[id]/aprovacao/cadeia-de-exploracao.test.ts` (c3d01a4): percorre os quatro passos da cadeia da auditoria numa compra de R$ 500 mil e verifica o corte em duas camadas.
- `src/app/api/slack/events/route.test.ts` e `src/app/api/contratos/import/route.auth.test.ts` (c3d01a4).

Infraestrutura:

- `src/app/error.tsx`, `src/app/global-error.tsx`, `src/app/not-found.tsx` (c532d09): telas de erro e 404.
- `src/lib/logger.ts` (c532d09): log JSON por linha, com redação de campos sensíveis e desempacotamento de Error. Sem teste.
- `src/lib/upload.ts` e `src/lib/upload.test.ts` (2606a7b): `validarAnexo()` com 15MB e 21 extensões, e 10 casos de teste.
- `src/lib/codigo.ts` e `src/lib/codigo.test.ts` (f25e745): `proximoCodigo()` atômico e 5 casos, incluindo 30 chamadas simultâneas.
- `prisma/migrations/20260819180000_indices_e_contador_de_codigo/migration.sql` (f25e745): 45 índices e a tabela `CodeCounter`.
- `src/lib/etapa.ts` e `src/lib/etapa.test.ts` (fe98882): `avancarEtapa()`, `notificarAvancoDeEtapa()`, `ConflitoDeEtapa`, e 6 casos de integração.

Interface e texto:

- `src/lib/rotulos.ts` (e1c0610, ampliado em 493e943): mapas de rótulo, helper `rotulo()`, `CAMPO_LABEL` com 42 chaves e helper `campo()`.
- `src/components/useAcaoRemota.tsx` (b7754f8): hook `useAcaoRemota()` e componente `StatusDaAcao` para retorno visível nos painéis administrativos.
- `src/lib/cnpj.ts` e `src/lib/cnpj.test.ts` (0d36474): `normalizarCnpj()` e `formatarCnpj()` (esta última sem consumidor), com 60 linhas de teste.

Auditoria, constraints e negócio:

- `prisma/migrations/20260819200000_auditoria_de_permissao/migration.sql` e `src/lib/auditoria-permissao.ts` (8aacf1a): tabela `PermissionChange` e helper de gravação.
- `src/app/api/users/[id]/route.auditoria.test.ts` (8aacf1a): 5 casos, nenhum cobrindo o caminho ALCADA.
- `prisma/migrations/20260819210000_constraints_de_coerencia/migration.sql` e `src/lib/constraints.test.ts` (e8e1375): 11 CHECK constraints e 6 casos que escrevem direto no banco.
- `src/lib/destinatarios.ts` (ca1e8aa): `DESTINO_CONTROLADORIA`, lido de `EMAIL_CONTROLADORIA`.
- `src/app/api/requests/[id]/triagem/route.fracionamento.test.ts` (ca1e8aa): 3 casos com o envio mockado.
- `prisma/migrations/20260819220000_persistir_orcamento_extra/migration.sql` e `src/app/api/requests/[id]/validacao-orcamentaria/route.orcamento-extra.test.ts` (7493bd7).
- `prisma/migrations/20260819203855_anexo_triagem_fornecedor/migration.sql` e `src/app/api/requests/[id]/attachments/route.categoria.test.ts` (4ff4047).

## O que ficou em aberto

### Depende de decisão ou ação do dono do sistema

- As seis brechas de autorização em rotas de leitura listadas em `docs/plano-autorizacao-leitura.md`, mais a sétima frente do prompt (chat da solicitação). Nada foi implementado. A mais grave é `GET /api/requests/[id]/ai-insight`, que devolve `anthropicApiKey` e `geminiApiKey` em texto puro dentro da resposta HTTP.
- Duas regressões introduzidas em e1c0610 e ainda presentes no HEAD: o `<select>` de status de chamado, que exibe sempre a primeira opção; e a prop `status` de `ContractActions`, que faz um contrato já cancelado exibir o formulário de cancelamento e permite reenviar e-mail e Slack para toda a Tesouraria, porque `src/app/api/contracts/[id]/route.ts` não tem guarda de status.
- O defeito de e8e1375: a constraint `Notification_canal_valido` não aceita "ERP", então `src/app/api/erp/purchase-requests/[id]/confirm/route.ts:55` falha depois de o `erpSyncedAt` já ter sido gravado.
- O buraco de semeadura do `CodeCounter` (f25e745): num banco que já tenha códigos emitidos, a primeira criação depois da migração colide com o `@unique`.
- `requiresBasicVendorScreening` continua sem chamador. A decisão registrada em 4ff4047 foi que a triagem de fornecedor informa e não bloqueia; ativar a regra ficou para depois.
- Trocas de gestor de centro de custo não entram na trilha de permissão (`GESTOR_DE_CENTRO_DE_CUSTO` sem call site), e o manual afirma o contrário.
- A trilha `PermissionChange` é write-only: nenhuma tela nem rota lê a tabela.
- `src/lib/workflow.ts:242` ainda traz `return 3; // Gerente F&NC + CEO`, e o achado n=7 do PDF não aponta para essa ocorrência.
- O PDF de auditoria está defasado: no HEAD marca 6 achados com situação, quando 13 já haviam sido atendidos ao fim do bloco P3. Os corpos dos achados n=1 e n=7 continuam com texto que hoje está errado.
- Os tempos por etapa de Mapa de Cotação em diante foram estimados com o time em ago/2026, sem medição, para revisar quando houver dado real via `StageEvent`.
- `formatarCnpj` sem consumidor: depois de 0d36474 o CNPJ aparece sem formatação em quatro telas.
- A guarda do comprovante de Orçamento Extra só vale no ramo em que `budgetOk` é false; e nenhuma tela lê `request.extraBudget`.
- Uma pessoa logada qualquer continua podendo baixar o Pedido de Compra e qualquer anexo de qualquer solicitação sabendo o id.
- "Seu chamado {code} foi registrada" em `src/components/NdaRequestForm.tsx:98`.
- Três linhas em branco residuais em `src/app/admin/acessos/page.tsx`; doc comment do `CodeCounter` engolido pelo do `PermissionChange` no schema.
- O traço médio U+2013 sobrevive em `RequestActions.tsx`, `dashboard-data.ts` e `pdf/pedidoCompra.tsx`.

### Depende de infraestrutura que ainda não existe

- Migração de backfill para os CNPJs já gravados em formato misto. Sem ela, o controle anti-fracionamento só vale para gravações novas, e o único ponto que compensa na leitura é `supplier-history`.
- Backfill de `dueAt` das Approvals existentes, calculadas em dias corridos antes de f55b66d.
- Tabela de feriados: `somarDiasUteis()` pula só sábado e domingo, e o JSDoc registra isso em vez de fingir precisão.
- `validarAmbiente()` só roda em quem importa `@/lib/auth`, então os dois crons e o middleware não passam pela conferência de boot.
- A neutralização de `LOCAL_BYPASS_AUTH` depende exclusivamente de `NODE_ENV === "production"`; staging ou preview que não sete assim continua obedecendo a flag.
- A revogação imediata de acesso só existe onde o banco é consultado. O middleware roda no edge e decide pelo cookie, e 23 das 57 rotas não citam nenhum caminho de sessão, então o token carrega papéis antigos até a renovação, limitada pelas 8 horas de `maxAge`.
- O cron de alerta de contrato não tem guarda de repetição: chamado duas vezes no mesmo dia, reenvia.
- As CHECK constraints são invisíveis ao `schema.prisma`, e um `migrate dev` futuro pode acusar drift.
- Cobertura de teste: 4 das 11 constraints são testadas; nenhuma rota de etapa tem teste do 409 de conflito; `mensagemDeSucesso()` é lógica pura e ficou descoberta; os caminhos de erro de `useAcaoRemota` (revert otimista, modal que não fecha, corpo não JSON, exceção de rede) não têm teste; o `logger.ts` e as três telas de erro não têm teste; o teto de 5 MB da importação de contratos não tem teste; o caminho ALCADA da trilha de permissão não tem teste.
- Testes de concorrência limitados pelo pool de conexões do Prisma: `etapa.test.ts` usa 3 chamadas simultâneas porque com 10 falhava de forma intermitente por esgotamento do pool, o que é limite de infraestrutura e não a propriedade sob teste.

## Erros cometidos no caminho

Esta seção registra onde a base enganou quem mexeu nela, e onde quem mexeu escreveu mais do que fez.

**Scripts de edição que reportaram sucesso sem alterar nada.** É o tropeço mais consequente da sessão. Em 402c2c8, a mensagem descreve a migração completa da rota de Aprovação, incluindo o guard atômico contra dois aprovadores decidindo ao mesmo tempo, e afirma que "todas as seis rotas com ramificação estão migradas". O diff é um arquivo e uma linha: um `import { avancarEtapa }` que fica sem uso. Nenhum comportamento mudou, e a corrida entre aprovadores de Nível 2/3, que gerava evento de histórico duplicado e e-mail de aprovado saindo duas vezes, continuou aberta por mais dois commits. A causa está confessada em 2d2607f: a migração rodou por um script que imprimia sucesso incondicionalmente, e um problema de escape de template literal fez as edições do corpo não pegarem. Nenhum teste teria pego, porque nada mudou. O padrão é recorrente o bastante para ter virado regra escrita: o prompt de execução de `docs/plano-autorizacao-leitura.md` proíbe explicitamente editar arquivo com script Node usando template literal, "porque nesta base isso já falhou em silêncio várias vezes: o script imprime sucesso e não altera nada".

**Varreduras declaradas como completas que não eram.** Três vezes na mesma sessão. Em 9c0f9b0 a mensagem afirma "Rewrote every occurrence across src/", mas `src/app/globals.css` não foi tocado e ficou com 42 ocorrências, só limpas em d678ba9. Em d678ba9 a mensagem afirma que "the repository now has zero occurrences", e restavam 16 linhas em `.env.example`, `.gitignore`, uma migration e `public/brand/alai-concepts.html`, limpas em cfa074d, cuja mensagem admite o motivo: "the earlier sweep filtered by extension and missed files that have none". Em f9521eb o título fala em manter o grep "at zero" quando o repositório não estava em zero nem antes nem depois. E em e1c0610 a mensagem afirma ter verificado o HTML servido e que nenhum enum aparece como texto visível em tela nenhuma, enquanto o painel de fallback de `RequestActions.tsx` continuava imprimindo o código cru da etapa, corrigido só em b384aec, cujo texto admite "missed by the earlier label pass".

**Regra que se autoviolava.** eb2e650 escreveu o travessão literal entre parênteses dentro da própria regra que o proíbe, levando o repositório de 16 para 17 linhas com o caractere, justamente no documento que declarava a limpeza concluída. Corrigido em f9521eb, nomeando o caractere por codepoint.

**Correções escritas antes de a base sustentá-las.** 1154a22 fechou o caminho sem `personifiedBy` na rota de aprovação, mas naquele momento `requireRole` ainda lia `LOCAL_BYPASS_AUTH` direto de `process.env`, e é essa flag que desliga a comparação com a sessão; a correção só passou a valer em produção 48 minutos depois, em cfa074d. 4d31db4 isentou `/api/slack` do middleware por ter autenticação própria enquanto `verifySignature` ainda estava fail-open; a combinação só ficou segura 34 minutos depois, em c3d01a4.

**Comentário desatualizado codificado como regra de banco.** e8e1375 escreveu a constraint `Notification_canal_valido` transcrevendo o comentário `// EMAIL | SLACK` do model, sem conferir o código: `confirm/route.ts:55` grava `channel: "ERP"` fora de try/catch, e a rota de confirmação do ERP passou a falhar depois de o `erpSyncedAt` já ter sido gravado. Nenhum dos 6 testes da migração exercita o caminho do ERP.

**Documento de auditoria atualizado antes do trabalho, não depois.** 405268f foi o primeiro commit do bloco P3 e marcou quatro achados. Os oito commits seguintes fecharam mais nove achados e nenhum reabriu o arquivo, então o PDF que existe para reportar situação nasceu errado justamente sobre isso.

**Números de mensagem de commit não sustentados pelo diff.** Vários, e vale desconfiar deles em geral: "30 das 57 rotas sem checagem própria" em 4d31db4 (contagem conservadora dá 24; o 57 confere); "32 models" em f25e745 (o schema tem 30); "roughly 25 empty states" em 3e2262f (o diff mexe em 13); "about 40" mensagens de erro em 493e943 (o diff corrige 27); "20 execuções concorrentes" em f25e745 (o teste versionado usa 30); e a afirmação de 2606a7b de que quatro PDFs órfãos foram movidos para fora de `public/`, que não está no diff e não poderia estar, porque a pasta é gitignorada.

**Trocas de texto que quebraram lógica.** e1c0610 aplicou rótulo em duas props que eram lidas como valor e não como texto. No `<select>` de status de chamado o efeito é só visual. Em `ContractActions` o efeito é comportamental: contrato cancelado passa a exibir o formulário de cancelar, e a rota aceita cancelar de novo, reenviando e-mail e Slack para toda a Tesouraria. E 7b5fc1c trocou o sujeito de uma frase sem acertar o particípio, deixando "Seu chamado {code} foi registrada" na tela de sucesso do NDA.

**Blocos inteiros sem teste novo.** Os cinco commits da migração das rotas de etapa (9f99b92, b3c2b88, 402c2c8, cc9a6fb, 2d2607f) não criaram nenhum teste; a garantia se apoia em `src/lib/etapa.test.ts`, que testa o helper e não as rotas, e a verificação usada foi grep por `stageEvent.create` solta. Foi exatamente nesse bloco que o script silencioso passou despercebido.

**Ambiente.** O banco de desenvolvimento inteiro foi apagado em 18/08 por um processo automatizado, com causa nunca identificada, o que motivou o guardrail de nenhuma escrita no banco sem autorização na hora, registrado em `docs/plano-correcao.md`. A sessão começou com o banco vazio e 26 migrations reportadas como não aplicadas, porque o schema foi recriado sem histórico. No Windows, operações do Prisma exigem parar o servidor antes, por causa do EPERM na dll do query engine. E em fe98882 o `testTimeout` do Vitest subiu de 5s para 15s porque num arranque frio o tempo de import passou de 50s para 118s e uma corrida inteira falhou por timeout sem nada de errado no comportamento.