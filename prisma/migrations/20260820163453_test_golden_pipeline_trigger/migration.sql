-- Migration de teste: valida o mecanismo de deteccao/execucao de migrations
-- da Golden Pipeline (dbMigrations) de ponta a ponta no onboarding do
-- acerto-compras. Nao altera nenhum schema de proposito -- e so um comentario,
-- seguro de aplicar em qualquer ambiente (dev/hml/svc) sem efeito colateral.
SELECT 1;
